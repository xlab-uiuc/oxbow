#include "test_global.h"
// #include "oxbow_libfs.h"
#include "test_set.h"

int main(void)
{
	file_basic_operation_test();
	// open_latency_test(1000);
	// sq_read_test(PAGE_SIZE);
	// sq_write_test(PAGE_SIZE);
	// fsync_lat_test(PAGE_SIZE);
	// mkdir_test();
	return 0;
}
