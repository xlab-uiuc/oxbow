## Troubleshootings

---

If `libcapstone-dev` package is not found,

```
sudo apt-add-repository universe
```

If `apt-add-repository` command is not found,

```
sudo apt-get install software-properties-common
```

---
